<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="/scores.css">
</head>
<body>
    <header>
        <h1 class="header-title">NBA Scores</h1>
        <nav>
            <ul>
                <li><a href="/">Home Page</a></li>
                <li><a href="/statistics">Statistics</a></li>
                <li><a href="/scores" class="active">Scores</a></li>
                <li><a href="/trending">Trending News</a></li>
                <li><a href="/teams">Teams</a></li>
                <li><a href="/predictions">Predictions</a></li>
                <li><a href="/contactus">Contact Us</a></li>
            </ul>
        </nav>
    </header>
    
    <main>
        <div class="date-navigation">
            <a href="/scores?date=<%= prevDay %>" class="nav-button prev-day">◀</a>
            <h2><%= date %></h2>
            <a href="/scores?date=<%= nextDay %>" class="nav-button next-day">▶</a>
        </div>

        <div class="scores-container">
            <% if (scores && scores.length > 0) { %>
                <% scores.forEach(game => { %>
                    <div class="game-card">
                        <div class="game-header">
                            <span class="game-date"><%= game.date %></span>
                            <span class="game-time"><%= game.time %></span>
                            <span class="game-status"><%= game.status %></span>
                        </div>
                        
                        <div class="teams-container">
                            <div class="team away <%= game.awayTeamWon ? 'winner' : '' %>">
                                <div class="team-logo">
                                    <img src="<%= game.awayTeamLogo %>" alt="<%= game.awayTeam %> logo">
                                </div>
                                <div class="team-name"><%= game.awayTeam %></div>
                                <div class="team-score <%= game.awayTeamWon ? 'winning-score' : '' %>"><%= game.awayScore %></div>
                            </div>
                            
                            <div class="team home <%= game.homeTeamWon ? 'winner' : '' %>">
                                <div class="team-logo">
                                    <img src="<%= game.homeTeamLogo %>" alt="<%= game.homeTeam %> logo">
                                </div>
                                <div class="team-name"><%= game.homeTeam %></div>
                                <div class="team-score <%= game.homeTeamWon ? 'winning-score' : '' %>"><%= game.homeScore %></div>
                            </div>
                        </div>
                        
                        <div class="game-venue">
                            <%= game.venue %><%= game.location ? ', ' + game.location : '' %>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="no-games">
                    <p>No games scheduled for this date.</p>
                </div>
            <% } %>
        </div>
    </main>
    

    
    <script src="/js/scores.js"></script>
</body>
</html>